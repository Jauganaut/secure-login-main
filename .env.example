# Cloudflare Pages Secret Binding Configuration
# This variable is accessed securely by the Pages Function at /api/discord.
# It MUST be set as an Environment Variable (Secret) in your Cloudflare Pages deployment settings.
# For local testing with `wrangler pages dev`, set it in your local .env file.

DISCORD_WEBHOOK_URL=https://discordapp.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN
